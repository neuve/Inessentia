---
import Base from '../../layouts/Base.astro';
import { sitePath } from '../../i18n/utils';

const locale = 'es';
const termsPath = sitePath(locale, 'terms');
---

<Base
  title="Costos | Inessentia"
  description="Tarifas claras para acompañarte. Individual, pareja y familia comparten el mismo costo y duración. Consulta el precio por sesión y el paquete de continuidad."
  altPath="/costs/"
>

  <!-- HEADER -->
  <!-- ver es/testimonios.astro: .container--read (760, centrado) desalineaba
       el H1 del borde canónico; se anula el padding de .page-header--soft y
       se pasa al patrón de una sola caja (max-width + padding) que ya usan
       las secciones de esta página (ver costos-grid más abajo). -->
  <header class="page-header--soft zone-dark" style="padding-left:0;padding-right:0;">
    <div style="max-width:var(--container-pad-w);margin:0 auto;padding:0 var(--gutter);box-sizing:border-box;">
      <p class="label-section">Costos</p>
      <h1>Invierte en tu proceso</h1>
      <!-- max-width:54ch: al salir de .container--read (760) la caja pasó a
           --container-pad-w (hasta 1600), y sin tope la entradilla se estiraba
           a una línea de lectura demasiado larga. Mismo tope que ya usa la
           entradilla de testimonios.astro. -->
      <p class="body-text" style="margin:20px 0 0;max-width:54ch;">Tarifas claras para acompañarte, ya sea que empieces hoy o sostengas tu proceso con continuidad.</p>
    </div>
  </header>

  <!-- BANDA: MISMO COSTO Y DURACIÓN -->
  <section class="zone-dark" style="background:transparent;">
    <div data-reveal style="max-width:var(--container-pad-w);margin:0 auto;padding:20px var(--gutter);box-sizing:border-box;display:flex;align-items:center;gap:14px;flex-wrap:wrap;">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--accent-canvas)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="flex:none;"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>
      <p style="margin:0;font-size:clamp(14px,1.1vw,16px);line-height:1.55;color:var(--on-canvas);">Individual, pareja y familia comparten el <strong>mismo costo</strong> y una duración de <strong>50 a 60 minutos</strong> por sesión.</p>
    </div>
  </section>

  <!-- TARJETAS DE PRECIO -->
  <section class="zone-dark" style="background:transparent;padding:clamp(48px,7vw,64px) 0 20px;">
    <div class="costos-grid" style="max-width:var(--container-pad-w);margin:0 auto;padding:0 var(--gutter);box-sizing:border-box;display:grid;grid-template-columns:1fr 1fr;gap:28px;align-items:stretch;">

      <!-- Por sesión -->
      <div data-reveal style="background:var(--surface);border:1px solid var(--border-card);border-radius:24px;padding:clamp(32px,4vw,44px) clamp(28px,3.5vw,40px);display:flex;flex-direction:column;box-shadow:0 24px 54px rgba(60,31,94,.10);">
        <p class="label-section">Por sesión</p>
        <h2 class="heading-section" style="font-size:var(--fs-h4);margin:0 0 28px;">Primera sesión y sesiones esporádicas</h2>
        <div style="margin-top:auto;">
          <div style="display:flex;align-items:baseline;gap:8px;">
            <span style="font-family:var(--ff-head);font-weight:700;color:var(--heading);font-size:clamp(42px,5vw,52px);line-height:1;">$1,700</span>
            <span style="font-weight:700;font-size:15px;color:var(--text-faint);">mxp*</span>
          </div>
          <a href="https://wa.me/patriciomx" target="_blank" rel="noopener" class="btn btn--gold" style="margin-top:26px;">Agenda cita<span class="btn__dot"></span></a>
        </div>
      </div>

      <!-- 4 sesiones -->
      <div data-reveal class="on-dark" style="position:relative;background:var(--purple);border-radius:24px;padding:clamp(32px,4vw,44px) clamp(28px,3.5vw,40px);display:flex;flex-direction:column;box-shadow:0 30px 64px rgba(60,31,94,.28);">
        <div style="position:absolute;top:26px;right:26px;background:var(--sand);color:#2e1b12;font-weight:800;font-size:12px;letter-spacing:0;text-transform:none;padding:7px 14px;border-radius:99px;">Ahorra 5%</div>
        <p class="label-section">Continuidad</p>
        <h2 class="heading-section" style="font-size:var(--fs-h4);color:#fff;margin:0 0 6px;">4 sesiones</h2>
        <p style="margin:0 0 28px;font-size:15px;line-height:1.5;color:rgba(255,255,255,.72);">Recurrencia semanal o quincenal.</p>
        <div style="margin-top:auto;">
          <div style="display:flex;align-items:baseline;gap:12px;flex-wrap:wrap;">
            <span style="font-family:var(--ff-head);font-weight:500;color:rgba(255,255,255,.55);font-size:26px;line-height:1;text-decoration:line-through;">$6,800</span>
            <span style="display:flex;align-items:baseline;gap:8px;">
              <span style="font-family:var(--ff-head);font-weight:700;color:#fff;font-size:clamp(42px,5vw,52px);line-height:1;">$6,398</span>
              <span style="font-weight:700;font-size:15px;color:var(--sand);">mxp*</span>
            </span>
          </div>
          <a href="https://wa.me/patriciomx" target="_blank" rel="noopener" class="btn btn--secondary" style="margin-top:26px;">Reserva tu pack</a>
        </div>
      </div>

    </div>
  </section>

  <!-- NOTA — zone-dark a propósito (no zone-light): en una página corta como
       esta, a esta altura el lienzo TODAVÍA es vino/magenta, no crema; en
       tema claro el override fuerza texto oscuro igual, así que zone-dark es
       la opción segura en ambos casos. -->
  <section class="zone-dark" style="background:transparent;padding:14px 0 clamp(56px,8vw,80px);">
    <div data-reveal style="max-width:var(--container-pad-w);margin:0 auto;padding:0 var(--gutter);box-sizing:border-box;">
      <div style="border-top:1px solid var(--border-hair);padding-top:22px;max-width:78ch;">
        <p style="margin:0 0 8px;font-size:14px;line-height:1.65;color:var(--on-canvas-faint);">* Los pagos se procesan por transferencia bancaria (SPEI) o Stripe. El pago anticipado confirma tu lugar; también puedes cubrirlo en efectivo el día de tu sesión.</p>
        <p style="margin:0;font-size:14px;line-height:1.65;color:var(--on-canvas-soft);">Consulta más detalles en los <a href={termsPath} class="link-underline">términos del servicio</a>.</p>
      </div>
    </div>
  </section>

</Base>

<style>
  @media (max-width: 720px) {
    .costos-grid { grid-template-columns: 1fr !important; }
  }
</style>
</content>
